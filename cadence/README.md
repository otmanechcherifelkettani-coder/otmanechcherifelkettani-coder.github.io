# Cadence

A new website project living at `/cadence` on this GitHub Pages site.

Static one-page site (`index.html` + `styles.css`, self-hosted fonts in `fonts/`), built against `DESIGN.md` — the single source of truth for all tokens, type, spacing, breakpoints and components.

Once merged to the default branch, it will be reachable at:
`https://otmanechcherifelkettani-coder.github.io/cadence/`
